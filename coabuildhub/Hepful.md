One more reusable trick discovered along the way: db.ascension.gg/?spell=<id>\&power returns clean JSON (name, icon, full tooltip) for any Ascension spell ID — so if your CoA work ever needs live tooltips for arbitrary IDs, that endpoint is the direct line. It's noted in Engram along with the rest.

